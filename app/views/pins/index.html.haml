#pins.transitions-enabled
	- @pins.each do |pin|
		.box.panel.panel-default
			= link_to (image_tag pin.image.url), pin
			.panal-body
				%h2= link_to pin.title, pin #go to pin path
				%p.user 
				Submitted by
				= pin.user.email